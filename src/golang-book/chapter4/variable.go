package main

import "fmt"

func main() {
    x := "hello world"
    fmt.Printf("Go compiler is able to infer the type based on the literal value you assign the variable\n")
    fmt.Printf("x=%s \n", x);

}
